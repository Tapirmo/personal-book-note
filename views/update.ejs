<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ("partials/header") %>
    <title>Update</title>
    <style>
      body {
        background-color: #f8f9fa;
      }
      .post-container {
        background-color: white;
        border-radius: 15px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        padding: 2rem;
        margin-top: 3rem;
      }
      .post-image {
        width: 100%;
        height: auto;
        max-width: 400px;
      }
      .form-container {
        padding: 1rem;
      }
      textarea {
        min-height: 120px;
      }
    </style>
  </head>
  <body>
    <%- include ("partials/nav") %>
    <div class="container post-container">
      <div class="row">
        <div
          class="col-md-6 d-flex align-items-center justify-content-center mb-4"
        >
          <img src="/img/post1.svg" class="post-image" alt="post" />
        </div>
        <div class="col-md-6 form-container">
          <h2 class="mb-4">修改書評</h2>
          <%- include ("partials/message") %>
          <form action="/profile/<%= foundPost._id %>?_method=PATCH" method="POST">
            <input type="hidden" name="_method" value="PATCH" />
            <div class="mb-3">
              <label for="title" class="form-label">書名：</label>
              <input
                name="title"
                type="text"
                class="form-control"
                placeholder="請輸入本書名稱"
                required
                value="<%= foundPost.title %>"
              />
            </div>
            <div class="mb-3">
              <label for="bookAuthor" class="form-label">作者：</label>
              <input
                name="bookAuthor"
                type="text"
                class="form-control"
                placeholder="請輸入本書作者"
                value="<%= foundPost.bookAuthor %>"
                required
              />
            </div>
            <div class="mb-3">
              <label for="content" class="form-label">內容：</label>
              <textarea
                class="form-control"
                name="content"
                placeholder="請摘要介紹書本內容"
                required
              >
<%= foundPost.content %></textarea
              >
            </div>
            <div class="mb-3">
              <label for="goodSentences" class="form-label">本書佳句：</label>
              <textarea
                class="form-control"
                name="goodSentences"
                placeholder="如果本書有佳句，可以寫在這裡"
              >
<%= foundPost.goodSentences %></textarea
              >
            </div>
            <button type="submit" class="btn btn-secondary btn-lg w-100">
              提交書評
            </button>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
